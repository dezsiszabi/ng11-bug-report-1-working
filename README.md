# Reproduction steps

- Clone the repo
- run `npm install`
- run `ng build angular-tour-of-heroes --prod`
- run `npm run start-lite`

The browser opens and shows the text:

```
test works!

Joe
Jane
```